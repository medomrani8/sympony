{% extends 'baseFront.html.twig' %}

{% block body %}
{% block contenpage %}


 <h4 class="card-title">|</h4> <h4 class="card-title">|</h4> <h4 class="card-title">|</h4>
<div class="site-wrapper site-layout--default">

    <!-- Header
    ================================================== -->
    
    <!-- Header / End -->

    <!-- Content
    ================================================== -->
    
    {{ form_start(connexion) }}
    <main class="site-content login-page" id="wrapper">
        <div  align="center">
            <div class="col-md-6">
                <h2 class="h4">Login</h2>
                <p></p><p></p><p></p>
                <form action="#" class="form login-form">
                    <div class="form-group">
                        <div >{{ form_row(connexion.email,{"attr":{},"label":"Email"}) }}</div>
                    </div>
                    <div class="form-group">
                        <div >{{ form_row(connexion.mdp,{"attr":{},"label":"Password"}) }}</div>
                    </div>
                    <div class="form-group d-flex justify-content-between align-items-center">

                        <span class="password-reminder">

<a href="{{path('app_forgot_password_request')}}">Forgot password?</a>
							</span>
                    </div>
                    <div >
                        <input type="submit" class="btn btn-primary text-center" value="login">
                    </div>
                    <div class="login-form__info">Do not have an account ?</div>
                    <div class="login-form__info">What are you waiting for?</div>

                    <a href="ajouteruser" class="btn btn-primary text-center">
                       &nbsp;</i>Sign Up
                    </a>

                </form>

            </div>
        </div>

        <div >
            {{ form_widget(connexion) }}
        </div>
        {{ form_end(connexion) }}
    </main>

{% endblock %}
{% endblock %}